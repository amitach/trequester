require 'spec_helper'

describe Trequester do
  it 'has a version number' do
    expect(Trequester::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
